channels:
  staging_openssl3: openssl3
  jcmorin-ana-org: aws

aggregate_branch: openssl3_builds
aggregate_check: false

build_parameters:
  - "--suppress-variables"
  #- "--skip-existing"
  - "--error-overlinking"
